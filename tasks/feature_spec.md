# Feature Spec

このファイルは、実装前の一時的な working spec だけを置く。
完了済みの仕様、設計判断、検証ログを履歴として蓄積しない。

## Rules

- 実装中の feature だけを簡潔に記述する
- 完了後に将来参照価値が残る判断は `docs/adr/` か `docs/specs/` へ移す
- 完了後に参照価値がない作業順メモ、検証ログ、履歴は削除する
- 再発防止ルールは `tasks/lessons.md` へ記録する

## Current Active Specs

- None
